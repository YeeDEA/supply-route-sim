"""supply_sim — importable modules extracted from the original Colab notebooks.

Each module's docstring names the notebook (now under notebooks/) it was
extracted from. The code is a faithful copy of the notebook code cells,
cleaned only of Colab-specific setup (!pip installs); logic is unchanged.

One exception: ``entities`` did not come from a notebook. It is the entity
model written into the ideathon planning document itself and was never run
-- see its docstring, and docs/proposal-code/ for the document's other code.
"""
